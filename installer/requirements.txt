# Runtime dependencies
customtkinter>=5.2.2
Pillow>=10.0.0
requests>=2.31.0

# Build-only (not bundled at runtime)
pyinstaller>=6.0.0

# Optional — used by make_assets.py for high-quality SVG conversion.
# Falls back to a Pillow-drawn logo if not installed.
# cairosvg>=2.7.0
# svglib>=1.5.1
# reportlab>=4.0.0
